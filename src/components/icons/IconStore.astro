---
import { withBase } from "../../utils/paths";

interface Props {
  class?: string;
}

const { class: className = "h-24 w-24" } = Astro.props;
---

<img src={withBase("/assets/svg/icon-minoristas.svg")} alt="" class={className} aria-hidden="true" loading="lazy" />
