---
import { withBase } from "../../utils/paths";

interface Props {
  class?: string;
}

const { class: className = "h-full w-full" } = Astro.props;
---

<img src={withBase("/assets/svg/icon-globe-hero.svg")} alt="" class={className} aria-hidden="true" loading="lazy" />
