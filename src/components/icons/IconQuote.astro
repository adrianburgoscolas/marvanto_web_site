---
import { withBase } from "../../utils/paths";

interface Props {
  class?: string;
}

const { class: className = "h-12 w-12" } = Astro.props;
---

<img
  src={withBase("/assets/marvanto_logo.png")}
  alt=""
  class={className}
  aria-hidden="true"
  loading="lazy"
/>
