---
import { withBase } from "../../utils/paths";

interface Props {
  class?: string;
}

const { class: className = "h-10 w-10" } = Astro.props;
---

<img src={withBase("/assets/svg/icon-phone.svg")} alt="" class={className} aria-hidden="true" loading="lazy" />
